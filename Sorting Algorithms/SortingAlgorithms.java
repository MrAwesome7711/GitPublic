public class SortingAlgorithms<E extends Comparable<E>>
{
    private E[] CustomSort(E[] startArray)
    {
        E[] tempArray = (E[]) new Comparable[(startArray.length*2)-1];
        E[] finalArray = (E[]) new Comparable[startArray.length];

        

        return finalArray;
    }
}